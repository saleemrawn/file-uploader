<% if (locals.errors) { %>
 <div class="bg-warning-subtle p-3 rounded-4 mb-3">
     <ul class="mb-0">
        <% errors.forEach(error => { %>
            <li><%= error.msg %></li>
        <% }); %>
     </ul>
 </div>
<% } %>